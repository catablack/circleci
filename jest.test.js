test('adds 1 + 2 to equal 3', () => {
  expect(1 + 2).toBe(3);
});

test('multiply 2 * 2 to equal 4', () => {
  expect(2 * 2).toBe(4);
});

test('multiply 2 * 3 to not equal 4', () => {
  expect(2 * 3).toBe(6);
});